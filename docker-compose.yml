services:
  hometab:
    build:
      context: .
      dockerfile: Dockerfile
    image: hometab-pilot:latest
    container_name: hometab-pilot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8088:8080"
    volumes:
      - hometab-data:/data

volumes:
  hometab-data:
