# 飞牛 OS / PVE 智能导航工作台设计计划

## 设计定位

项目不做单纯的网址导航，而是做一个“家庭服务器驾驶舱”。iTab 的优点是打开新标签页即可搜索、看组件、点网址；我们要保留这种低摩擦入口，同时把飞牛 OS、PVE、容器和虚拟机状态前置，让首页一眼判断系统是否健康。

核心主角：家庭服务器健康状态。

次级能力：常用服务导航、容器管理、PVE 虚拟机观察、告警和快捷动作。

## 功能布局

### 1. 顶部全局区

- 时间、日期、问候语和当前网络环境。
- 全局搜索栏：支持搜索网址、容器、虚拟机、NAS 文件入口、书签。
- 快捷命令入口：输入 `restart qb`、`open pve`、`logs emby` 等命令可直达动作。
- 告警摘要：例如磁盘接近阈值、PVE 节点 CPU 偏高、容器重启次数异常。

### 2. 左侧空间导航

- 总览：健康状态、快捷访问、告警。
- 飞牛 OS：存储池、共享目录、Docker/应用、网络、远程访问。
- PVE：节点、虚拟机、LXC、快照、备份任务。
- 容器：运行状态、端口、镜像版本、日志、启动/停止/重启。
- 网页导航：按分类管理网址，兼容 iTab 风格图标宫格。
- 自动化：备份、同步、定时任务、Webhook。
- 日志中心：系统日志、容器日志、PVE 任务日志。

### 3. 首页主视觉

- 左上大卡片：家庭服务器健康分，突出“是否需要处理”。
- 中间：飞牛 OS 状态，包括 CPU、内存、温度、存储、网络、Docker 服务。
- 右侧：PVE 状态，包括节点负载、运行 VM 数、备份状态、虚拟机列表。
- 下方：容器管理列表，显示服务名、镜像、端口、状态、资源占用、快捷操作。
- 右下：导航图标区，保留 iTab 的“常用入口”体验。

### 4. 飞牛 OS 管理能力

- API / SSH 连接配置：主机地址、Token、SSH Key、只读模式。
- 状态展示：CPU、内存、系统盘、存储池、温度、网络上下行、在线时长。
- Docker / 应用联动：容器列表、端口、镜像版本、重启策略、健康检查。
- 文件服务状态：SMB/NFS/WebDAV/远程访问状态。
- 风险提醒：硬盘 SMART 警告、存储池容量、Docker 服务异常、远程访问断开。

### 5. PVE 管理能力

- PVE API Token 接入，默认只读，管理动作需二次确认。
- 节点状态：CPU、内存、磁盘、温度、集群状态、订阅状态。
- VM / LXC 列表：运行状态、IP、CPU/内存占用、磁盘、备份时间。
- 快捷动作：打开控制台、启动、关机、重启、创建快照、查看备份。
- 拓扑视图：PVE 节点、VM、飞牛 OS、容器、暴露端口关系。

### 6. 网页导航兼容能力

- 支持 iTab 式图标宫格、文件夹、分组、搜索引擎切换。
- 支持导入浏览器书签或手动维护 JSON 配置。
- 每个网址可绑定健康检查，例如 HTTP 200、端口连通、登录页可达。
- 服务类网址可自动关联容器或 VM，例如 qBittorrent 关联 `qbittorrent` 容器。

### 7. 我补充的关键点

- 权限分级：访客只看导航，管理员可看状态，维护者可执行启停。
- 只读安全模式：默认所有 API Token 只读，破坏性动作需要显式确认。
- 秘钥管理：Token 不进前端包，放后端环境变量或本地加密存储。
- 审计日志：记录谁在什么时候重启了哪个容器或虚拟机。
- 离线降级：API 挂了时，导航仍可用；状态卡片显示最后更新时间。
- 健康检查策略：服务可用性、端口、容器健康、VM Agent 状态分开判断。
- 移动端场景：手机上优先显示告警、搜索、常用服务和一键打开远程访问。
- 数据刷新节奏：状态 5-10 秒刷新，日志手动刷新，导航不频繁请求。

## 信息架构建议

首页只展示最重要的 6 类信息：

- 当前是否健康。
- 飞牛 OS 是否正常。
- PVE 是否正常。
- 哪些容器/VM 正在运行。
- 有哪些需要处理的告警。
- 我现在最可能要打开哪个服务。

不建议首页塞满所有指标，SMART 详情、完整日志、历史曲线都放到二级页，否则会失去导航页“打开即用”的轻快感。

## 技术实现计划

### 第一阶段：静态原型

- 建立首页布局、颜色、组件、移动端响应式。
- 用 mock 数据展示飞牛 OS、PVE、容器和导航入口。
- 完成配置 JSON 结构设计。

### 第二阶段：后端连接

- Node.js / Go 后端提供统一 API。
- 飞牛 OS：优先走官方/可用 API；不足部分用 SSH 只读命令补齐。
- PVE：使用 Proxmox VE API Token 拉取 node、qemu、lxc、tasks、storage。
- 容器：Docker API 或通过飞牛 OS 接口代理读取。

### 第三阶段：管理动作

- 容器启动、停止、重启、查看日志。
- PVE VM 启动、关机、重启、快照、控制台跳转。
- 所有高风险动作加确认和审计日志。

### 第四阶段：导航增强

- iTab 风格分组、搜索引擎切换、图标上传、文件夹。
- 服务健康检查和状态徽标。
- 书签导入、配置导出、备份恢复。

## 推荐视觉方向

风格名称：晨雾机房。

整体不是纯深色黑板，也不是普通白色后台，而是带轻微暖色晨雾背景的玻璃控制台。这样既呼应 iTab 的背景图氛围，又能承载运维信息。主色使用墨蓝、松石绿和琥珀色：绿色表示健康，琥珀色表示提醒，红色只用于真正风险。

## 页面草图说明

- 左侧固定导航栏，像“空间切换器”。
- 顶部是搜索和告警，保持 iTab 的新标签页效率。
- 第一屏大卡片显示健康分和关键指标。
- PVE 与 FNOS 并排，形成双系统视图。
- 容器/VM 列表采用运维表格，但做成卡片式，不显得像传统后台。
- 右下保留常用网址图标网格，让它仍然是导航项目。

