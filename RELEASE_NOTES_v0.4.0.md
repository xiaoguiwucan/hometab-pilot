# HomeTab Pilot v0.4.0

v0.4.0 是操作安全和可恢复性增强版本，围绕真实 Docker / PVE 运维场景补齐安全确认、更新中心、备份中心、审计和通知告警能力。

## 功能新增

- 操作安全加强：危险操作加入冷却时间，Docker 停止和重建提供撤销窗口。
- 操作前摘要：停止/重建容器前展示镜像、挂载目录、端口、环境变量和重建风险。
- 容器更新中心增强：展示本地 digest、远端 digest，标记有新镜像的容器。
- 一键安全更新：仅批量更新可安全复原的容器，批量操作要求再次输入管理密码。
- 风险提示：识别 `devices`、`cap_add`、`host network`、`privileged` 等难以自动复原的配置。
- 备份中心升级：备份包含版本、备注、来源，支持自动定时备份、备份对比和全部备份压缩包下载。
- 审计细化：前端完整展示审计列表，支持 Docker / PVE / 登录 / 通知筛选，支持 JSON 和 CSV 导出。
- 通知更智能：支持告警静默窗口、每日摘要和通知通道测试结果明细。
- 首页可用性：容器搜索、状态筛选、更新筛选、容器置顶，PVE 实例搜索和置顶。
- 部署体验：新增 v0.2.0/v0.3.0 升级到 v0.4.0 教程、FNOS Compose 示例和健康检查排错说明。

## Docker 镜像

```bash
docker pull xiaoguiwucan0426/hometab-pilot:0.4.0
docker pull xiaoguiwucan0426/hometab-pilot:v0.4.0
docker pull xiaoguiwucan0426/hometab-pilot:latest
```

## 升级提示

- 保留原 `/data` 数据卷即可继承 v0.3.0 的书签、登录、连接和通知配置。
- 升级后建议先导出一次备份，再启用自动定时备份。
- 容器批量更新前请先执行“检查全部更新”，确认只有安全容器进入批量更新列表。
